# Stashive frontend

The Vue application provides the Stashive interface. The repository foundation includes
the application shell and a small API connectivity status view.

Run it from the repository root:

```bash
npm --prefix frontend run dev
```

The Vite development server proxies `/api` requests to `http://localhost:8000`.
